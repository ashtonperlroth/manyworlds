/**
 * Markdown rendering configuration for Many Worlds.
 * 
 * TODO: Implement a configured ReactMarkdown component that:
 * - Uses remark-gfm for GitHub Flavored Markdown (tables, strikethrough, etc.)
 * - Uses react-syntax-highlighter with a custom theme matching our code-bg/code-text tokens
 * - Wraps output in the .markdown-content class (styles defined in globals.css)
 * - Handles code blocks with language detection and copy button
 * - Renders links with target="_blank" and rel="noopener"
 * 
 * Export a <MarkdownRenderer content={string} /> component.
 */

export {};
