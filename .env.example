# Many Worlds — Environment Variables
#
# API keys are stored in the browser (localStorage), NOT in .env files.
# This file exists only for documentation and if you ever add server-side features.
#
# No environment variables are required to run the app.
